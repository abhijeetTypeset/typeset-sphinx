# paths
input-dir=testdata 
output-dir=output

# top-level tests
tests=spec_04.yml

# flags
generate-classes=true
debug-mode=true

# implicit function names
page-implicit=atPage
interm-implicit=canSee
control-implicit=contains, equals, startsWith, endsWith, empty
requires-data=contains, equals, startsWith, endsWith, type